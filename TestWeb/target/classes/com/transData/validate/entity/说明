校验这里类关系说明

1.dojo为与数据库相关联的javabean。其中ValidateRuleData.java与rule中的ValidateRuleEntity.java为一对一对应关系。entity中提供通过data的转化方法
2.DataValidateEntity与ValidateEntityStock为一对一对应关系，DataValidateEntity与ValidateRuleEntity为一对多关系。ValidateEntityStock是DataValidateEntity的业务封装类，并保存校验返回结果。
3.实际业务处理将只关心ValidateEntityStock，ValidateEnum，ValidateRuleEntity,ValidateResult这四个类。其他类为这四个类持久化的过渡性数据结构。ValidateUtil为执行工具
4.DataValidateEntity实际上为过渡性数据，不应该是dojo，它有一个经过封装后的ruledata子集。以后迭代时可以考虑将这部分拆析出来。